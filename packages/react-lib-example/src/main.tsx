import React from 'react'
import ReactDOM from 'react-dom/client'
import ChainRoot from './ChainRoot.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ChainRoot />
  </React.StrictMode>,
)
